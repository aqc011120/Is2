from django.apps import AppConfig

class RelecloudConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'  # Configura BigAutoField como la clave primaria predeterminada
    name = 'relecloud'
